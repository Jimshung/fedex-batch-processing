#!/usr/bin/env bash

# 自動切換到 intl-shipping-serena 專案配置
echo "🔄 切換到 intl-shipping-serena 配置..."
gcloud config configurations activate intl-shipping-serena

# 顯示當前配置狀態
echo "📊 當前配置:"
gcloud config list --format="table(account,project,region,zone)"
